# Megatron Bridge Boundary

`megatron.bridge` is the source of truth for the bridge API.
Orbit-owned customizations should live under `patches/bridges`,
`patches/low_precision`, `patches/conversion`, and `patches/peft`
as the package boundary evolves.
